services:
  - type: web
    name: guardia-security-system
    runtime: node
    plan: free
    buildCommand: npm run postinstall
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
